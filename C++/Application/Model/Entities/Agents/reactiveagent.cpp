#include "reactiveagent.hpp"

//Constructeur
ReactiveAgent::ReactiveAgent(InfoAgent* info, RABody* body, SNZ_Model* model) 
	: Entity(info, body, model) {
}

//Destructeur
ReactiveAgent::~ReactiveAgent(){
}
